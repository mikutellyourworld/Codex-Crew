# scrub-allowlist.txt — paths/patterns excluded from the scrub-lint scan.
# Each line is a grep -v pattern. Lines starting with # are comments.
# Add entries here for intentional stubs that reference internal names.

# Auth stubs (OSS placeholders for Amazon-internal auth flows)
src/codex_crew/browser/auth.py
src/codex_crew/browser/setup.py
src/codex_crew/browser/screencast.py
src/codex_crew/dashboard/handlers/mwinit.py
src/codex_crew/dashboard/handlers/__init__.py
src/codex_crew/dashboard/handlers_system.py:.*midway
src/codex_crew/dashboard/handlers/messaging.py:.*Midway
src/codex_crew/dashboard/server.py:.*midway
src/codex_crew/dashboard/server.py:.*mwinit
src/codex_crew/midway.py

# Brazil-aware runtime detection (checks if installed via brazil or pip)
src/codex_crew/slack/gateway.py:.*[Bb]razil
src/codex_crew/mcp_discovery.py:.*brazil

# Deny-pattern strings and security config (reference internal names as blocklist)
src/codex_crew/dashboard/state.py:.*brazil
src/codex_crew/config/defaults.json

# Eval test scenarios (fictional stories using internal tool names as test data)
src/codex_crew/eval/scenarios/

# Code comments referencing provenance CRs (historical context, not leaking anything)
src/codex_crew/agent.py:.*Amazon-specific Apollo/Brazil
src/codex_crew/apps/backend.py:.*Brazil
src/codex_crew/context.py:.*CR-
src/codex_crew/eval/runner.py:.*CR-
src/codex_crew/seed.py:.*CR-
src/codex_crew/slack/gateway.py:.*CR-

# Frontend tests referencing brazil-build as test input
website/src/test/RunInTerminalBtn.test.tsx
website/src/test/sensitiveCommand.test.ts
# Frontend source/test comments referencing CRs for provenance
website/src/.*CR-

# Documentation (contains internal references that will be scrubbed/removed
# before publish). Anchored to the top-level ``docs/`` tree only — ``website/``
# (incl. ``website/docs/``, the shipped frontend pack-author contract) is a
# public source root and IS scanned, so a bare ``docs/`` must not swallow it.
^docs/
CHANGELOG.md

# CLAUDE.md's "do not reintroduce" policy paragraph names the internal marker
# words (Midway, mwinit, brazil, taskei, meshclaw, AIM) as the denylist itself —
# that self-documenting line must be allowed to name what it forbids.
CLAUDE.md:.*internal marker names

# The matching "Never re-add" list lives in
# ``docs/system-specs/oss-fork-boundaries.md``, covered by the ``^docs/`` entry
# above for the same reason: the rule forbidding an internal service has to be
# able to name the service. AGENTS.md itself only links to that doc, so it needs
# no anchor here and a NEW internal reference in it is caught.

# This script and its allowlist
scripts/scrub-lint.sh
scripts/scrub-allowlist.txt

# Self-test fixture for the review-marker test/ pass: the file exists only
# while ``./scripts/scrub-lint.sh --test`` runs, planted to prove that an
# allowlisted entry is honoured. Anchored to the WHOLE reported line
# (^path:lineno:exact-probe-text$), so the only line this can ever drop is the
# harmless probe text itself — a line that carries the review id PLUS any other
# content (an internal URL, a real marker) does not match and still fails the
# gate, and so does any other marker in this path.
^test/__scrub_test_marker_allowed\.py:[0-9]*:# test marker: review CR-9999999$

# Security path-blocklists: ``.midway`` is blocked so the agent cannot read a
# credential dir an enterprise companion may create (a security feature, kept).
website/src/utils/safePath.ts:.*\.midway
src/codex_crew/sandbox.py:.*\.midway

# Back-compat SSO-marker default + legacy profile alias (see platform/profile.py):
# the enterprise companion's managed launcher may still use these.
src/codex_crew/platform/profile.py:.*\.midway

# Legacy CloudFormation stack/bucket names in the migration guide — renaming
# them would make the find/delete remediation commands point at nothing.
src/codex_crew/deploy/skills/artifact-deploy/MIGRATION.md:.*meshclaw

# Deploy scan module (detects internal content - the regex IS the feature)
src/codex_crew/deploy/scan.py

# macOS Apple bundle id (io.github.mikutellyourworld.codexcrew): the app's signed identity, baked
# into every shipped .app and ONBOARDED with the code-signing service per exact
# identifier. Renaming it is a coordinated task (re-onboard the signing identity
# first), NOT a string scrub — kept intentionally. Not a secret (README.md).
packaging/signing/generate-manifest.py:.*com\.amazon\.kiro\.crew
packaging/signing/manifest-template.json:.*com\.amazon\.kiro\.crew
packaging/signing/sign-dmg.sh:.*com\.amazon\.kiro\.crew
packaging/build-desktop.sh:.*com\.amazon\.kiro\.crew
docs/build/signing-runbook.md:.*com\.amazon\.kiro\.crew
# Same signed identity, in the computer-use self-denylist: CodexCrew's own
# dashboard can flip this feature's primary enable, edit the denied-command
# opt-out and change approval mode — all reachable ONLY out-of-band precisely so
# the agent cannot reach them. Refusing our own bundle as an automation target is
# what stops the agent from clicking its way around those keystone protections,
# so the id must be matched literally. Anchored to the one file that needs it.
src/codex_crew/computer_use/policy.py:.*com\.amazon\.kiro\.crew

# Public AWS documentation URLs (docs.aws.amazon.com / aws.amazon.com) — generic
# AWS product references, not internal couplings.
src/codex_crew/cli_cloud.py:.*aws\.amazon\.com/cli
src/codex_crew/cloud/aws.py:.*docs\.aws\.amazon\.com
src/codex_crew/cloud/ssm.py:.*docs\.aws\.amazon\.com

# Code-of-conduct enforcement contact. ``redacted-email`` is
# the PUBLISHED intake for the Amazon Open Source Code of Conduct
# (https://aws.github.io/code-of-conduct), printed in that public policy and in
# every Amazon-published OSS repo that adopts it. It is a public-facing address,
# not an employee alias or an internal coupling, and the policy is unusable
# without naming where reports go. Line-anchored so a NEW @amazon.com address
# anywhere in this file is still caught.
CODE_OF_CONDUCT.md:.*opensource-codeofconduct@amazon\.com

# Security-issue intake contact. ``redacted-email`` is the
# monitored shared mailbox that receives vulnerability reports; the project-domain
# address it replaces is not a monitored inbox. Like the code-of-conduct address
# above it is a published, public-facing intake — SECURITY.md is unusable without
# naming where reports go — not an employee alias or an internal coupling.
# Line-anchored so a NEW @amazon.com address anywhere in this file is still caught.
SECURITY.md:.*kiro-crew-security-support@amazon\.com

# Live public AWS service class-name string the dashboard credit pill calls
# (CodeWhisperer RTS GetUsageLimits) — a functional API identifier, not a leak.
src/codex_crew/dashboard/handlers/kiro_usage_api.py:.*codewhisperer

# Aperture feedback-collection API, the backend proxy target for the
# session-pulse survey. Despite the ``.aws.dev`` suffix (which INTERNAL_PATTERN
# matches broadly), this is a PUBLIC ingestion endpoint that external,
# non-Amazon users already submit through — the "public-api" segment in the
# hostname names that explicitly, and it was curl-verified reachable and
# functional from outside Amazon's network. Line-anchored to the two literal
# endpoint constants so a genuinely internal ``*.aws.dev`` host added elsewhere
# in this file is still caught.
src/codex_crew/dashboard/handlers/feedback.py:.*ingestion\.aperture-public-api\.feedback\.console\.aws\.dev
src/codex_crew/dashboard/handlers/feedback.py:.*prompt\.aperture-public-api\.feedback\.console\.aws\.dev

# code_review_sage CR-URL parser tests: synthetic CR-12345678 ids and a
# code.amazon.com sample URL are the parser's asserted test fixtures.
src/codex_crew/apps/builtins/code_review_sage/tests/

# Deploy tests use AWS example account 123456789012 (well-known placeholder)
test/test_deploy_web_profiles.py
test/test_deploy_web_handlers.py
test/test_deploy_web_iam.py

# The ``~/.aim`` skill-discovery scan in agent.py (``_all_skill_paths`` and the
# managed-MCP path note) is OSS-inert — no such tree exists on a vanilla install
# — and carries its own TODO(aim-governance follow-up) to route through the
# ``McpToolingProvider.extra_skills()`` CPP seam. That move is deferred to its
# own PR because of the security-sensitive symlink-resolution and sensitive-path
# gating around it.
#
# These MUST stay path+pattern anchored, never a bare ``src/codex_crew/agent.py``:
# the allowlist is applied as a plain ``grep -v`` to BOTH the internal-marker
# scan and the employee-alias scan, so a path-only entry would drop every line of
# this 2000+ line file from BOTH gates — a future ``code.amazon``/``brazil-build``
# /``CR-######``/alias in agent.py would then ship with CI green, exactly the
# failure mode AIM_PATTERN was added to close.
# Remove these entries when that follow-up lands.
src/codex_crew/agent.py:.*~/\.aim
src/codex_crew/agent.py:.*aim mcp install
src/codex_crew/agent.py:.*AIM skills
src/codex_crew/agent.py:.*AIM-installed
src/codex_crew/agent.py:.*AIM install neutralized
src/codex_crew/agent.py:.*Path.home() / "\.aim"
src/codex_crew/agent.py:.*pkg / "\.aim"

# Two more pre-existing, OSS-inert ``.aim`` reads that the path-BUILDING half of
# AIM_PATTERN newly surfaces (the ``~/.aim`` literal alone missed them). Both are
# the same deferred follow-up as agent.py above, allowlisted line-anchored so the
# gate still catches a NEW one:
#   - slack/handler.py: reads ``~/.aim/cc-plugins`` for cc-plugin agent names.
#   - dashboard/handlers/files.py: ``.aim`` is one element of a directory-SKIP set
#     (a file-indexer exclusion), so it is an inert name, not a coupling.
src/codex_crew/slack/handler.py:.*~/\.aim/cc-plugins
src/codex_crew/slack/handler.py:.*Path.home() / "\.aim" / "cc-plugins"
src/codex_crew/dashboard/handlers/files.py:.*"\.codexcrew", "\.kiro", "\.aim"

# Public AWS console deep-link in the Ops Mission Control CloudWatch adapter.
# ``<region>.console.aws.amazon.com`` is the PUBLIC AWS console — the same domain
# any external AWS customer uses — and is matched only because INTERNAL_PATTERN
# carries a broad ``amazon\.com`` alternative. Allowlisted line-anchored to the
# console host so a genuinely internal ``*.amazon.com`` reference added to this
# file is still caught.
src/codex_crew/apps/builtins/ops_mission_control/backend/providers/cloudwatch.py:.*console\.aws\.amazon\.com
# ...and its test, which asserts the console host is what a validated region produces and
# that an injected one cannot move it. Same public domain, same line-anchored narrowing.
src/codex_crew/apps/builtins/ops_mission_control/tests/test_providers.py:.*console\.aws\.amazon\.com
