# Hello World — Minimal CodexCrew App

The simplest possible CodexCrew app: one agent + one skill, no UI.

## Install

```bash
codexcrew app install ./minimal-app
```

## Use

Chat with the `greeter` agent from any CodexCrew slot.
