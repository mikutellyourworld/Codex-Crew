{
  "name": "codexcrew",
  "description": "Autonomous personal AI agent — schedules recurring tasks, spawns parallel workers, learns from corrections, and operates independently via the dashboard, CLI, and messaging channels",
  "model": "auto",
  "tools": [
    "execute_bash",
    "fs_read",
    "fs_write",
    "code",
    "grep",
    "glob",
    "web_fetch",
    "web_search",
    "introspect",
    "session",
    "report",
    "tool_search",
    "@codexcrew-cron",
    "@codexcrew-core",
    "@codexcrew-computer"
  ],
  "allowedTools": [
    "fs_read",
    "code",
    "glob",
    "grep",
    "web_fetch",
    "web_search",
    "introspect",
    "session",
    "report",
    "@codexcrew-cron/cron_list",
    "@codexcrew-cron/cron_pause",
    "@codexcrew-cron/cron_resume",
    "@codexcrew-cron/cron_trigger",
    "@codexcrew-cron/cron_remove",
    "@codexcrew-cron/cron_remove_all",
    "@codexcrew-core"
  ],
  "resources": [
    "file://.kiro/steering/**/*.md"
  ],
  "includeMcpJson": false,
  "hooks": {
    "auto_approve_tools": [
    ],
    "postToolUse": [
      {
        "matcher": "execute_bash",
        "command": "{ echo \"$(date -u +%Y-%m-%dT%H:%M:%SZ) BASH:\"; cat; echo; } >> \"${CODEXCREW_HOME:-$HOME/.codex-crew}/audit.log\""
      }
    ]
  }
}
